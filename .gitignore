node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# IDE
.idea/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report
/.cache

# Coverage
/coverage

# Rust
/target
# cargo-mutants run output
/mutants.out
/mutants.out.old
# cargo-fuzz build output + run corpora/crashes (targets + Cargo.toml are tracked)
/crates/*/fuzz/target
/crates/*/fuzz/corpus
/crates/*/fuzz/artifacts

# Generated wasm-bindgen glue — rebuilt by `just wasm-build` and in CI, never
# vendored (no source/wasm drift). The hand-written index.ts / diagnostics.ts /
# env.d.ts in the same directory stay tracked.
/crates/audit-wasm/pkg
/src/lib/wasm/degree_audit.js
/src/lib/wasm/degree_audit.d.ts
/src/lib/wasm/degree_audit_bg.wasm
/src/lib/wasm/degree_audit_bg.wasm.d.ts

# Real transcripts (PII: name, student ID, birthdate, university name — never commit)
*.local.pdf
/crates/**/tests/fixtures/real/
